name: "GitHub Pages - Python Sphinx - with PDF"

on:
  workflow_call:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout repository"
        uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab

      - name: "Setup Python"
        uses: actions/setup-python@7b9ef6fc5a7a74dfb7c0924eb42615d7af1e4de5
        with:
          python-version: "3.x"
          cache: "pip"
          cache-dependency-path: "pyproject.toml"

      - name: "Install dependencies"
        run: pip install .[docs]

      - name: "Install latexmk"
        run: |
          sudo apt-get update -y
          sudo apt-get install -y latexmk texlive-latex-extra # texlive-latex-recommended texlive-fonts-recommended

      - name: "Build docs"
        run: |
          cd docs 
          make latexpdf
          REPO_NAME=${{ github.event.repository.name }}
          cp build/latex/$REPO_NAME.pdf source/_static/$REPO_NAME.pdf
          make dirhtml

      - name: "Upload artifact"
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./docs/build/dirhtml

  deploy:
    environment:
      name: "github-pages"
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    needs: build
    steps:
      - name: "Deploy to GitHub Pages"
        id: deployment
        uses: actions/deploy-pages@v1
